// 8. Filter Unique Elements
// Using a for-of loop, filter and print only unique elements from an array.
console.log('js 8 running')

const items = [1, 2, 2, 3, 4, 4, 5];
let temp = 0;
for(let x =0;x<6; x++){
  
}